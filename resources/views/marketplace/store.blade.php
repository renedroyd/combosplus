@extends('layouts.marketplace')
@section('title',$tenant->name.' — CombosPlus')
@section('content')
<section class="bg-slate-950 text-white"><div class="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><p class="text-sm font-semibold text-blue-300">Tienda en CombosPlus</p><h1 class="mt-2 text-4xl font-semibold">{{ $tenant->name }}</h1><div class="mt-3 text-sm text-slate-300"><span class="text-amber-400">★ {{ number_format((float)$tenant->rating,1) }}</span> · {{ $tenant->review_count }} opiniones</div><p class="mt-5 max-w-2xl text-slate-300">{{ $tenant->description ?: 'Descubre el catálogo de esta tienda.' }}</p></div></section>
<section class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><h2 class="sr-only">Productos</h2><div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">@forelse($products as $product)<a href="{{ route('marketplace.product',[$tenant,$product->getKey()]) }}" class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm hover:shadow-lg"><div class="aspect-[4/3] bg-slate-100">@if($product->image)<img src="{{ asset('storage/'.$product->image) }}" alt="{{ $product->name }}" class="h-full w-full object-contain" loading="lazy">@endif</div><div class="p-5"><h2 class="font-semibold">{{ $product->name }}</h2><div class="mt-2 flex justify-between text-sm"><b>{{ '$'.number_format((float)$product->price,2) }}</b><span class="text-amber-500">★ {{ number_format((float)($product->rating ?? 0),1) }}</span></div></div></a>@empty<p class="col-span-full py-16 text-center text-slate-500">Esta tienda todavía no tiene productos publicados.</p>@endforelse</div>
<div class="mt-16 max-w-3xl"><h2 class="text-2xl font-semibold">Opiniones de clientes</h2><div class="mt-6 space-y-4">@forelse($reviews as $review)<article class="rounded-2xl border border-slate-200 p-5"><div class="flex items-center justify-between gap-4"><span class="text-amber-500">★★★★★ <span class="text-sm text-slate-500">{{ $review->rating }}/5</span></span>@if($review->verified_purchase)<span class="text-xs font-semibold text-emerald-600">Compra verificada</span>@endif</div><p class="mt-3 text-slate-700">{{ $review->comment }}</p><p class="mt-3 text-xs text-slate-500">{{ $review->user?->name ?: 'Cliente' }} · {{ $review->created_at?->diffForHumans() }}</p></article>@empty<p class="rounded-2xl border border-dashed border-slate-300 p-6 text-slate-500">Todavía no hay opiniones publicadas.</p>@endforelse</div></div></section>
@endsection