@extends('layouts.marketplace')
@section('title',$product->name.' — '.$tenant->name)
@section('content')
<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><a href="{{ route('marketplace.store',$tenant) }}" class="text-sm font-semibold text-blue-600">← {{ $tenant->name }}</a><div class="mt-8 grid gap-10 md:grid-cols-2"><div class="aspect-square overflow-hidden rounded-3xl bg-slate-100">@if($product->image)<img src="{{ asset('storage/'.$product->image) }}" alt="{{ $product->name }}" class="h-full w-full object-contain">@else<div class="grid h-full place-items-center text-slate-400">Sin imagen</div>@endif</div><div class="self-center"><p class="text-sm font-semibold uppercase tracking-wider text-blue-600">{{ $tenant->name }}</p><h1 class="mt-2 text-4xl font-semibold">{{ $product->name }}</h1><div class="mt-4 text-amber-500">★ {{ number_format((float)($product->rating ?? 0),1) }}</div><p class="mt-6 text-3xl font-bold">{{ '$'.number_format((float)$product->price,2) }}</p><p class="mt-6 leading-7 text-slate-600">{{ $product->description ?: 'Producto disponible en esta tienda.' }}</p><div class="mt-8 flex flex-col gap-3 sm:flex-row"><a href="{{ route('marketplace.product.buy',[$tenant,$product->getKey()]) }}" class="inline-flex items-center justify-center rounded-xl bg-blue-600 px-6 py-3 font-semibold text-white">Comprar en esta tienda</a><a href="{{ route('marketplace.store',$tenant) }}" class="inline-flex items-center justify-center rounded-xl border border-slate-300 px-6 py-3 font-semibold text-slate-700">Ver tienda</a></div></div></div>
<div class="mt-16 max-w-3xl"><h2 class="text-2xl font-semibold">Opiniones sobre este producto</h2><div class="mt-6 space-y-4">@forelse($reviews as $review)<article class="rounded-2xl border border-slate-200 p-5"><div class="flex items-center justify-between gap-4"><span class="text-amber-500">★★★★★ <span class="text-sm text-slate-500">{{ $review->rating }}/5</span></span>@if($review->verified_purchase)<span class="text-xs font-semibold text-emerald-600">Compra verificada</span>@endif</div><p class="mt-3 text-slate-700">{{ $review->comment }}</p><p class="mt-3 text-xs text-slate-500">{{ $review->user?->name ?: 'Cliente' }} · {{ $review->created_at?->diffForHumans() }}</p></article>@empty<p class="rounded-2xl border border-dashed border-slate-300 p-6 text-slate-500">Todavía no hay opiniones publicadas.</p>@endforelse</div></div></section>
@endsection