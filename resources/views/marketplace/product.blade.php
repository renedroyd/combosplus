@extends('layouts.marketplace')
@section('title',$product->name.' — '.$tenant->name)
@section('content')
<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><a href="{{ route('marketplace.store',$tenant) }}" class="text-sm font-semibold text-blue-600">← {{ $tenant->name }}</a><div class="mt-8 grid gap-10 md:grid-cols-2"><div class="aspect-square overflow-hidden rounded-3xl bg-slate-100">@if($product->image)<img src="{{ asset('storage/'.$product->image) }}" alt="{{ $product->name }}" class="h-full w-full object-contain">@else<div class="grid h-full place-items-center text-slate-400">Sin imagen</div>@endif</div><div class="self-center"><p class="text-sm font-semibold uppercase tracking-wider text-blue-600">{{ $tenant->name }}</p><h1 class="mt-2 text-4xl font-semibold">{{ $product->name }}</h1><div class="mt-4 text-amber-500">★ {{ number_format((float)($product->rating ?? 0),1) }}</div><p class="mt-6 text-3xl font-bold">{{ '$'.number_format((float)$product->price,2) }}</p><p class="mt-6 leading-7 text-slate-600">{{ $product->description ?: 'Producto disponible en esta tienda.' }}</p><a href="{{ route('marketplace.store',$tenant) }}" class="mt-8 inline-flex rounded-xl bg-blue-600 px-6 py-3 font-semibold text-white">Ver tienda</a></div></div></section>
@endsection